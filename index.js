

function setup(){
    createCanvas(windowWidth, windowHeight);
    pageHeader = createElement('h1', 'overskrift med CreatElement')
    pageHeader. position(100, 100)
    Select()
 }

function draw(){
    background(220)
    Fill('red')
    ellipse(20, 30, 40)
}
